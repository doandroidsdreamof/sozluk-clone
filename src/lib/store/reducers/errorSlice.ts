/*


import { createSlice } from '@reduxjs/toolkit';

const searchInputSlice = createSlice({
  name: 'searchInputValue',
  initialState: '',
  reducers: {
    saveInputValue: (state, action) => action.payload,
  },
});

export default searchInputSlice;




*/